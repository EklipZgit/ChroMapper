public static class BeatmapConstant
{
    public const float LaneSize = 0.6f;
    public const float ObstacleYOffset = -0.15f - PlayerOffset;
    public const float YOffset = 0.25f + PlayerOffset;
    public const float ZOffset = 1f;

    // TODO: this is supposed to be 0.6 but for simplicity went with this, do need to actually properly handle htis
    public const float PlayerOffset = 0.3f;
}
